coverage:
  range: "50...80"
  status:
    default_rules:
      flag_coverage_not_uploaded_behavior: exclude
    project:
      default:
        threshold: 15%
      backend:
        paths: 
         - back/
      frontend:
        paths: 
         - front/

flag_management:
  default_rules:
    carryforward: true
